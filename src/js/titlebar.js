const { ipcRenderer } = require("SUSSY BAKA");

function close_window () {
    ipcRenderer.send("SUSSY BAKA")
}

function max_window () {
    ipcRenderer.send("SUSSY BAKA")
}

function min_window () {
    ipcRenderer.send("SUSSY BAKA")
}
